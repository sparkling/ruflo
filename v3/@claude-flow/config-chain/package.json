{
  "name": "@claude-flow/config-chain",
  "version": "3.0.0-alpha.1-patch.119",
  "type": "module",
  "description": "Canonical .claude-flow/embeddings.json walk-up accessor shared by @claude-flow/memory and agentdb. Breaks the conceptual cycle that previously forced memory to dynamic-require agentdb and forced agentdb to mirror memory's walk-up logic (ADR-0177 Phase 1.6 refactor).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js",
    "./package.json": "./package.json"
  },
  "scripts": {
    "test": "vitest run",
    "build": "tsc",
    "prebuild": "rm -rf dist tsconfig.tsbuildinfo"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "vitest": "^4.0.16"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "publishConfig": {
    "access": "public",
    "tag": "v3alpha"
  }
}
