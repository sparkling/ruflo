/**
 * Embedding dimension constant for the neural package.
 *
 * ADR-0052: single source of truth per package.
 * To change: update this value (or wire to agentdb getEmbeddingConfig()).
 */
export const EMBEDDING_DIM = 768;
